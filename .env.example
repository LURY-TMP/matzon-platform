# ========== DATABASE — PostgreSQL (Consistency Layer) ==========
DATABASE_URL=postgresql://matzon:matzon_dev_2026@localhost:5432/matzon

# ========== DATABASE — MongoDB (Velocity Layer) ==========
MONGODB_URI=mongodb://matzon:matzon_dev_2026@localhost:27017/matzon?authSource=admin

# ========== JWT — RS256 ==========
JWT_SECRET=matzon-dev-secret-change-in-production
JWT_PRIVATE_KEY_PATH=./keys/private.pem
JWT_PUBLIC_KEY_PATH=./keys/public.pem
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# ========== APPLICATION ==========
NODE_ENV=development
API_PORT=4000
WEB_PORT=3000
CORS_ORIGIN=http://localhost:3000
